# Broadlink IR Controller Configuration
# Rename this file to '.env' and fill in your details

# ========================
# Broadlink Device Settings
# ========================

# Device IP (find in your router admin or Broadlink app)
BROADLINK_IP="192.168.1.100"

# Device MAC address (no colons, find in Broadlink app)
BROADLINK_MAC="a0b1c2d3e4f5"

# Device type (hex code):
# - 0x2712 = RM mini 3
# - 0x27c2 = RM pro
# - 0x51bc = RM4 pro
BROADLINK_DEVTYPE="0x2712"

# ================
# Web Server Setup
# ================

# Server binding (0.0.0.0 = allow LAN access)
HOST="0.0.0.0"

# Server port (default: 5000)
PORT="5000"

# ======================
# Security Recommendations
# ======================
# For production use:
# - Keep HOST=0.0.0.0 only for local network access
# - Add authentication middleware
# - Use HTTPS with reverse proxy